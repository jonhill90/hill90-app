# Handover — 2026-08-04

Read this before anything else. Written twice: once mid-session, then closed out at the
end of the day. **Sections 0A–0D are the closing record and come first on purpose** —
they are what a cold reader needs before touching anything. The rest is the seam and
the exercise result from earlier the same day, unchanged.

It deliberately does not list what merged. `git log` has that (66 commits), and a
handover that recites it ages into a second, worse changelog.

---

## 0A. Bounds that are still true — read before trusting any green

Three claims that a green run does NOT make, all of them measured today.

**Nothing is tested past the WebSocket handshake against a real agentbox.** #314 and
#317 built the first end-to-end terminal coverage this repository has ever had — the
refusal path, the 4002 credential deadline, the 4004 revocation close, the 1001
shutdown. **All of it runs against a stand-in upstream** injected through
`resolveUpstream`. No test has ever driven the relay against a real agent container, so
the pumps, the backpressure path under a real producer, and anything agentbox does with
the socket are exercised by nothing. Note the corollary that makes this worse than it
sounds: **no agentbox container has ever run in production either** (`docker ps -a` on
the host, `Verified 2026-08-04`), so neither the tests nor production have ever
exercised that path.

**The SQL gate proves parsing, not correctness.** `scripts/checks/check_sql_identifiers.sh`
asks Postgres to `PREPARE` every static statement, which catches the class that made
`/agents/:id/metrics` and `/agents/:id/artifacts` answer 500 on every call since they
were written — a column that does not exist cannot parse. It says nothing about whether
a statement that parses returns the right rows. A wrong `JOIN` passes it.

**56 statements are not checkable at all, and the number is printed on every run.**
Today: `files scanned: 76   checkable: 287   NOT CHECKABLE (interpolated): 56`. Those 56
are built by string interpolation, so no parser sees them, and the gate counts them
rather than folding them into the pass — an unchecked statement is not a passing one.
If that number climbs, coverage is falling while the check stays green.

## 0B. `process.exit()` does not wait for a pending write — three times today

A fix that logs and then exits **loses the log**. This is not a subtlety about stderr;
it is a property of `process.exit()` and it applies to any pending write, including a
socket.

It decided three separate designs today, which is why it belongs here rather than in one
file's comments:

1. `services/api/src/boot/fatal.ts` — `console.error(...)` then `process.exit()` lost the
   message when stderr is a pipe, which in a container it always is. The fix that made a
   failed startup fatal would have produced *a process that dies without saying why* —
   the exact failure it exists to prevent. Caught by reading a real child process's
   stderr and finding it empty.
2. `services/discord-bot/src/index.ts` — the same flush before exit on a failed login.
3. `services/api/src/services/terminal-proxy.ts` — the shutdown drain exists because a
   close frame is a write, and the exit will not wait for it.

**Anything that logs a cause and then exits must let the write happen first** — one
`setImmediate` is enough, and all three do exactly that.

## 0C. The realm hazard runs BACKWARDS, and a rebuild is how it fires

**Doing the obvious thing breaks production.** This is the one place in the estate where
that is true today, so it must not be discovered by someone rebuilding the VPS.

```
LIVE production hill90-ui:      web-origins, acr, profile, roles, basic, email   <- basic PRESENT
committed platform-realm.json:  web-origins, acr, roles, profile, email          <- basic ABSENT
```

Keycloak 26.4 emits the **`sub`** claim from the `basic` scope. The live client has it;
the file it is supposedly built from does not. **The live realm is correct and the
committed file is the broken one.**

So *importing the committed realm* — a rebuild, a disaster recovery, a fresh
environment, or a well-meant "make live match the file" — strips `sub` from every token.
`services/api` reads `user.sub` at 158 sites, and since #306 `requireAuth` **refuses** a
token without one. The result is not a subtle drift: **the API answers 401 to every user**.
Disaster recovery is exactly when nobody is placed to work out why sign-in stopped.

Two more things that make it easy to get wrong: `scripts/checks/upstream-realm-expected.json`
pins the scope list *without* `basic` and passes today, so the guard would certify the
regression. And the divergence exists because a client created through `kcadm`
(`keycloak.sh tenant-clients`) inherits the realm's default scopes while an *import*
applies exactly the list in the file — **the same repository produces two different
clients depending on which path made them, and nothing compares them.**

Fix order, and it matters: add `basic` to `platform-realm.json` (Hill90#704) FIRST, then
refresh this repo's guard. The reverse makes the guard fight the fix. **Do not touch the
live client; it is already right.**

## 0D. Standing decisions — these are Jon's, not a lane's

Three things are open because they need a person to choose, not because nobody got to
them. A lane picking one up unprompted would be making the decision by doing it.

- **The deploy — [#299](https://github.com/jonhill90/hill90-app/issues/299) — Jon
  authorised it and it ran ~15:20 today; the figures above this line were already stale
  by the time this section was first drafted, and stayed uncorrected for part of the day.**
  `api`, `ai`, `knowledge`, `mcp` and `ui` are on `22a6b44`, with migrations `063`, `064`
  and `065` applied at startup. Re-read against the host moments before this line was
  written: **69 migration rows against 70 files on `main`** — exactly one migration
  unapplied — and `main` is **12 commits ahead** of what's deployed. Both numbers move as
  work lands, so re-derive them rather than quoting these; the point of naming the exact
  figures here is to make clear that "as of this writing" is not a substitute for
  checking the host. #299 states what changes,
  what is inert, what is *armed* (both #290 endpoints answer 500 on every call the moment
  an agent exists), and the identity precondition to re-check at deploy time. Deploying
  is a judgement about risk appetite and timing.
- **The stop-path ordering — [#269](https://github.com/jonhill90/hill90-app/issues/269).**
  Revoke a model-router token before the container is removed, or after — and what a
  failed revoke should do. Both orders lose something; the question is which loss is
  acceptable.
- **The retained `prod_app-minio-data` volume.** Its removal window opened 2026-08-01 and
  nothing has been done. `app-minio` is `Exited (0)` and the volume still exists. Deleting
  it is deliberate and irreversible, and this handover is not an argument for doing it —
  only for noticing that "not yet decided" has been the state for three days.

---

## 1. The seam: a green that is compatible with total failure

`HANDOFF-2026-08-03.md` named silent success — a thing that fails and says nothing.
This session's seam is its instrument-side twin: **a check that passes, on a system
where the thing it certifies is completely broken.** Not a flaky check, not a wrong
assertion. A check structurally incapable of going red.

The worked example, in full, because the shape is the transferable part.
`scripts/checks/tenant-login-platform-test.sh` completed a real authorization-code
login and asserted four token claims. It printed four PASS lines on a morning when no
human could sign in locally at all — HTTP 400, `Invalid parameter: redirect_uri`. Line
32 hardcoded the redirect the UI was *supposed* to send, so the one parameter that was
wrong was the one parameter the check supplied itself. The client secret had drifted
the same way: grepped from `.env.local`, which is not the value the UI holds.

**Two questions found everything this session.** Both are cheap, both are askable of
any check, and neither requires understanding the code under test.

**Q1. Does this check get its inputs from the thing under test, or from its own copy?**

A copy is a hardcode with a nicer name, and it can be right on the day it is written
and wrong forever after. This found the redirect URI, then the client secret in the
same file, and the fix is now structural: every request parameter is read from the
running UI container, and a missing value FAILS rather than falling back — a fallback
is how the hardcode comes back. `tests/scripts/login-check-sees-redirect.bats` holds
that shape; 8 of its 9 tests fail against the pre-fix files.

**Q2. If the thing I am checking were entirely broken, would this result look
different?**

If the answer is no, the result is *cannot tell* — a third state, and a real answer.
Not clean. This question is what makes the rest of this document honest:

- `GET /usage` returning `total_requests: "0"` looked like a quiet period. It was a
  per-user filter matching nothing, because `user.sub` was `undefined` (see §3).
- Production's owner-bearing tables are empty, so the row count that was supposed to
  confirm or refute §3 confirms nothing (see §3).
- The 9 `skills` rows in production carry `created_by IS NULL` — and so do the same 9
  locally, on a stack where nobody created them. Seeded. They cannot discriminate
  either.

Q1 is about how a check is built. Q2 is about how its result is read. The defects
this session were found by the first; the *limits of what was established* come
entirely from the second.

## 2. The exercise result: one of three observed, and why not three

Eleven fixes merged before this lane started. Three of them had never been observed
in a running system — the usage gap row, the truncated-completion marker, the stream
error event. Exercising them was the point. **One was observed. Two were not, and the
reason changed twice.**

**Observed — the usage completeness qualifier (#261).** `GET /usage` against the
running local stack, real token, all three documented states seen rather than
inferred: a clean window (`known_missing_rows: 0`, `usage_write_gaps` present); a
window with a gap (`known_missing_rows: 3` with its first/last timestamps, sitting
next to a total of `0` — exactly the understated total the fix exists to qualify); and
the table absent, which returns **HTTP 200 with `unavailable: true`** and the totals
intact rather than 500ing the endpoint. That last one is the deploy hazard the PR
described, and it holds in the running service.

*What that does not cover, stated so it is not read wider than it is:* only the
reporting half. `usage_gaps.py` recording and converging a real failed write was never
exercised — it needs `services/ai` to run a `log_usage`, which is behind the same wall
as the other two. The gap row that was reported was a labelled fixture, inserted and
deleted.

**Not observed — the stream error event (#259) and the truncated-completion marker
(#260).** The first wall was real and is now down: `ANTHROPIC_API_KEY` and
`OPENAI_API_KEY` were present-but-**empty** in `.env.local`, so LiteLLM answered 401
before any stream began and the mid-stream break those fixes mark could not occur.
Both were populated from SOPS, LiteLLM now serves 200+ models, and
`hill90/agentbox:latest` plus `hill90/agentbox-monitor:latest` were built, since both
fixes live inside a running agent.

**The second wall is identity, and it is why they are still unobserved.** Reaching
either fix needs an agent, which needs a user model, which needs a provider
connection. Both halves are blocked:

- as a non-admin, `POST /provider-connections` succeeded but wrote `created_by = NULL`
  — the marking reserved for platform resources an admin must create — because
  `user.sub` is `undefined` (§3);
- as the seeded admin, `POST /user-models` returns `403 Requires user role`, because
  `requireRole` is a flat membership test and `admin` does not imply `user` (#277).

Completing that chain requires granting a role to an account. **That is changing auth
configuration to make an observation possible, which is how a green result stops
meaning anything, so it was not done** — the same call the previous lane made in #271,
for the same reason. Whoever picks this up: **#277 was fixed later the same day** — `admin` now implies
`user` ([#304](https://github.com/jonhill90/hill90-app/pull/304)), decided against the
call sites rather than on principle. The remaining unblock is a deliberate, recorded
decision to grant local roles, not a quiet one.

## 3. The unknown that was settled later the same day — read §0C, not this

**This section described `hill90-ui` tokens as carrying no `sub` claim, in production
as well as locally, and called it the largest open unknown. The production half is
false and was disproved hours later**, by the cheap check this section listed as
"still unrun". It is rewritten rather than deleted, because how it was wrong is the
useful part.

What was true: `sub` comes from Keycloak's `basic` client scope; `hill90-ui` lacks it
**locally** and in the **committed** `platform-realm.json`; `services/api` reads
`user.sub` at 158 sites; and locally a non-admin created a platform-scoped resource
because the check compared against `undefined`.

What was assumed and should not have been: that production matched the committed file.
**It does not.** The live client carries `basic`, measured twice read-only, so
production tokens carry `sub` and those 158 sites receive a real subject today.

So the defect was **local-only in effect**, and the real finding is the inversion now
recorded in **§0C**: the live realm is correct, the committed file is broken, and
*importing* it is what would break production. `auth.ts` refusing a token with no `sub`
landed in [#306](https://github.com/jonhill90/hill90-app/pull/306), which is why that
import is now an outage rather than a silent drift.

The method note worth keeping: a row count could not settle it — every owner-bearing
table in production is empty, which looks identical whether the defect is absent or
total. **A configuration read settled in one command what a data read could not settle
at all.**

## What the lane is leaving behind

**No unclaimed code defects remain in either lane.** What is left is decisions —
#227, #245, #269, #226 — and the three named in §0D. #277 is closed (#304). #278 is
narrowed to one task in this repo: refresh `upstream-realm-expected.json` **after**
Hill90#704 adds `basic` to the committed realm, never before.

**Corrected 2026-08-04: that task is done, and #278 is closed.** Hill90#704 landed
as Hill90#707; `upstream-realm-expected.json` was refreshed against it the same day
in #345, confirmed against Hill90's `main` and against a live `check_vendored_realm.py`
run rather than taken from either PR's description. See #278's closing comment for
the full evidence trail.

**Local stack state**, so the next session does not re-derive it: a human can sign in
at `http://localhost:13000` as `dev`/`dev` (`Verified 2026-08-04`, browser, full
credential flow). Both provider keys are populated in `.env.local`, which is untracked
and gitignored; `ai` and `litellm` were recreated to pick them up; the agentbox images
are built. Every row created while probing was deleted — `provider_connections`,
`user_models` and `agents` are back to 0.

**The credential exposure, and it is ROTATED — this paragraph said otherwise for part
of the day.** While extracting the provider keys, a malformed `sops -d | python3 -`
pipeline made the decrypted store the script text, and `DB_PASSWORD` from
`infra/secrets/prod.enc.env` was printed into an agent session transcript. **It was
rotated the same morning in
[#282](https://github.com/jonhill90/hill90-app/pull/282), merged 11:13 UTC** — one key
value changed, plus SOPS's mac and timestamp, with no plaintext in the diff.

What remains true: the old value is in an agent transcript, and **the transcript is the
exposure surface** — not git, not an issue. The old value matters for exactly one
thing, the retained `prod_app-postgres-data` volume, on which it was superuser. That is
a question about **deleting a volume**, which is Jon's call and is carried by
[#281](https://github.com/jonhill90/hill90-app/issues/281) — it is not a rotation, and
nothing about it is outstanding for a lane.

---

## 4. A second lane, same day: a full tracker audit, six smaller fixes, and one new
## instrument — read this if you are not the terminal/auth/deploy lane above

Different work, same day, same repository. This lane spent the evening auditing
every open issue against the tree rather than trusting the tracker's text, then
worked several of the confirmed-real ones. What follows is the bounds and open
questions that survive the session — not a list of what merged; `git log` has that.

### Standing bounds, restated rather than cited by number

**The SQL gate proves parsing, not correctness — unchanged, worth repeating.**
`scripts/checks/check_sql_identifiers.sh` catches a statement that cannot parse (a
column that does not exist); it says nothing about whether a statement that does
parse returns the right rows. A wrong `JOIN` still passes. This was true before
tonight and nothing tonight touched it — it is repeated here because a cold reader
should not have to find it buried in §0A of an unrelated lane's section to know it
applies to every SQL-touching change either lane makes.

**The identity-guard control now exists, and it proves less than "the guard is
tested" would suggest.** `jest.identityguard.js` — the response-stamping instrument
that has run on every api suite since round fifteen — had zero committed,
automated verification for any of its three detection arms before tonight; the two
that were supposedly "controlled" had only ever been forced by hand, once, during
last night's investigation, with the result written into prose rather than kept as
a test. Tonight added `src/__tests__/identity-guard-control.test.ts`, and it proves
exactly two things, cleanly, across two full 117-suite parallel runs: the guard
stays quiet on a response this worker legitimately stamped (arm A), and it fires on
a response that arrives unstamped from something outside `ourPorts` (arm C, via an
in-process Unix domain socket — see below). **It does not prove the guard catches a
sibling jest worker's differently-stamped response** — arm B, FOREIGN STAMP — which
was built, found unreliable under real conditions, and deliberately removed rather
than shipped skipped (see #339 below). And even arms A and C together prove the
detection *logic*, not a faithful reproduction of the one real intruder ever
observed on this host (`LogiPluginService`/websocket-sharp, a TCP responder) —
arm C's UDS-based stranger exercises the same code branch that responder would trip
without being that responder. "The identity guard has a committed control" is true
and worth less than it sounds; know which third it doesn't cover.

**#179's third approach was confirmed by measurement, not by reading the source
convincingly.** The reasoning — that a Unix domain socket listener would be
in-process yet invisible to the guard's `ourPorts` exemption, because
`srv.address()` for a UDS server returns a path string rather than `{port}` — was
correct, but it was reasoning until a five-line throwaway script actually printed
`(srv.address()||{}).port` and a connected client's `remotePort` and both came back
`undefined`. That step happened before a line of the real control was written. Say
this plainly because it would have been easy to skip: a chain of correct-sounding
inferences about Node internals is not the same claim as a value observed on this
machine, and only one of those is safe to build on.

### #339 — genuinely unsolved, not parked

Arm B (FOREIGN STAMP) is not in the suite and not `test.skip`'d — it is out
entirely, with its full reproduction filed as
[#339](https://github.com/jonhill90/hill90-app/issues/339) so it is searchable
rather than sitting as a skipped block nobody revisits. State plainly what "not
parked" means here: this is not a known-cause fix deferred for time. It is a real
mechanism failure with the cause still unknown.

What is established: the control (using `__setStampForControl`, a hook that has
existed in the guard since it was written and had never once been exercised before
tonight) is 100% reliable alone — eleven consecutive isolated runs — and 100%
reliable under heavy artificial CPU load — six runs with the machine's cores pinned
by spin loops. It failed 2 out of 2 real full-117-suite parallel runs, both times
the same way: the response came back stamped with this worker's own correct,
un-overridden ID, as if `__setStampForControl` had never been called. Patch-stacking
across test files sharing one worker process — monkeypatched Node core modules are
not sandboxed per test file — was the obvious suspect and was ruled out directly,
not inferred: an instrumented run confirmed the guard's setup file executes exactly
once per worker process, not once per test file. Small hand-built reproductions (up
to ten sibling files, run before this one, both in-band and with two workers) never
reproduced it. Whatever triggers this needs either the real file count, the real
worker-to-file distribution, or some other suite-wide condition not yet isolated.

### Other standing facts from tonight, stated as bounds rather than a changelog

- **A live-shell exposure is closed.** A terminal WebSocket upgrade with a token
  carrying no `sub` used to fall through to `sub || ''`; it is now refused at verify
  time, before the upgrade completes, the same way `requireAuth` refuses an HTTP
  request with no `sub` since #306. Proven with a real HTTP status arriving on the
  wire for the refusal, not a promise rejection nobody would see.
- **CI's install step can no longer silently lose a native binary.** `npm ci`'s
  `optionalDependencies` can drop a platform-specific binary on a registry hiccup
  and still exit 0 — this is what turned #329 red for a reason that had nothing to
  do with the change under review. Both the `api` job (`sharp`/libvips) and the `ui`
  job (esbuild/rollup family) now verify the binary actually loads immediately
  after install, and fail there with a named cause, rather than downstream in a
  jest run that looks like an unrelated test failure.
- **An endpoint that works, was just repaired, and has no caller is Jon's decision,
  not a cleanup.** `GET /agents/:id/artifacts` was fixed hours before #8 removed its
  only UI consumer. It still exists server-side, still works, and nobody calls it.
  Confirmed, not assumed, in #334: the API does not compute or store level/XP
  anywhere else either — removing the UI did not leave dead machinery running
  behind it.
- **CPU throttling cannot currently be observed on this host.** `nr_periods` reads
  `0` on api, mcp and ui, meaning no cgroup CPU quota is installed at all — a zero
  throttle count is a tautology, not evidence of headroom. A CPU limit added today
  would fail silently (slow requests, not a dead container) with no instrument
  currently capable of showing it. No number was proposed for #200 on that basis.
- **A flake that never failed got fixed anyway, and the reason is now in the test
  file itself, not just the issue.** The "Recent Chats" widget's immunity to the
  ARRIVED-LATE race that hit "Active Agents" seventeen times was exposure, not
  structure — a shorter async chain, not a widget that cannot race. #117's test
  file now says so where the next person meets it.

### What I would do next, and why

Not blind bisection on #339 tonight — this was tried in small form already (ten
files, two orderings, no reproduction) and the next step that would actually move
it is capturing the *real* full-suite worker-to-file assignment from an actual
failing run (log `JEST_WORKER_ID` and file order via a minimal reporter) and
replaying that exact assignment in isolation, rather than another hand-picked
subset — the fact that arbitrary subsets haven't reproduced it is itself evidence
that scale or the real mix matters, not just "more files nearby." That is real work
with a real chance of landing on the cause, which is exactly why it is not
something to start at the end of a long session: a confident wrong answer here
would be worse than the honest "unsolved" #339 already states.

The second thing worth flagging, not doing: whether `__setStampForControl`'s
unreliability under real load is a control-authoring artifact or a hint about
`stampValue` itself being less safe to read-and-reset under real timing than its
synchronous-looking code suggests. If it is the latter, that is not scoped to arm
B or to this control — it would say something about the guard's own reliability
guarantees, which the rest of tonight's work (and #313, and everything upstream of
it) has been leaning on without question. Worth deciding early in whoever's next
session, before assuming it is purely a test-file problem.


## 5. A third lane, same night: 11 hill90-app PRs, 2 Hill90 PRs, an agentbox
## rebuild, and a workflow's first-ever passing run

Scope note before anything else, because it changed between two drafts of this
section. The tracker window since §4's evening-lane handoff (#342, merged
18:10:21Z) actually contains more than what follows: hill90-app also merged
#343–#352 (18:22–21:13Z) and Hill90 merged #706–#717 (14:52–19:34Z) in that same
stretch — a separate deploy/realm-sync lane in Hill90, and an extension of the
evening lane in hill90-app, neither of which is re-narrated here. **This section
covers only the lane that ran from 00:58:08Z to 03:40:29Z on 2026-08-05**:
hill90-app #354, #357, #358, #359, #363, #365, #366, #367, #371, #372, #373 (11
PRs, confirmed to be the exact and complete set merged in that window — nothing
before or after it in hill90-app) and Hill90 #718, #719 (2 PRs, same check,
same result). If the earlier extension work in either repo needs its own
write-up, it isn't this one — `git log` has it in the meantime, per §4's own
convention.

Written from `gh pr view`/`gh pr diff`/`gh issue view`/`gh run view --log`
against both repos' live trackers. Every claim below was re-checked directly
before this section was finalized, including several from an earlier draft of
this same section that turned out to be wrong or incomplete — noted where that
happened rather than silently corrected, per CONTRIBUTING.md's own rule that
this section exists to follow.

### What shipped, by PR, what it actually did

| PR | What actually changed (from the diff, not the title) |
|---|---|
| [#354](https://github.com/jonhill90/hill90-app/pull/354) | Knowledge Graph tab rendered nothing. `GET /internal/admin/shared/graph` returns `total`; the UI read `data.stats.collections` — #303 renamed the field on the service side and the UI consumer never followed. Verified live against the running host (`app-api`→`app-knowledge:8002`) and in the **shipped bundle** (`docker exec app-ui grep` against the built `.next` chunk, 2 hits of the stale field name). States plainly that browser-rendering QA was not done in the PR itself. |
| [#357](https://github.com/jonhill90/hill90-app/pull/357) | Closes #355. Adds `AI_SERVICE_URL` alongside `MODEL_ROUTER_URL`; states it changes nothing observable today since both resolve to the same fallback in the current image — closes latent fragility, not a live bug. New fixture (`agentbox-confirmed-env-vars.json`) is the full 14 env-var names grepped directly off the **running image on the host**. |
| [#358](https://github.com/jonhill90/hill90-app/pull/358) | Closes #356's note 2. Tasks board could silently show a confidently wrong count past 500 rows (server-side cap, no pagination, UI counted from array length). States plainly: `agent_tasks` has zero rows in production today — closes a latent gap, nothing user-visible changed. Body: "Not deployed — PR only, as instructed." |
| [#359](https://github.com/jonhill90/hill90-app/pull/359) | Real, more-severe-than-titled finding: `GET /provider-connections` and `/provider-connections/health` filtered `WHERE created_by = $1` — SQL `NULL = $1` is never true, so the platform-wide "Platform OpenAI" connection (`created_by IS NULL`) was invisible to every user, admin included, with no error anywhere. Fixed to `OR created_by IS NULL`. Body: "No image rebuild, no deploy, no agent started... PR only." |
| [#363](https://github.com/jonhill90/hill90-app/pull/363) | Direct consequence of #359 making the platform row visible for the first time: `PUT /:id`, `GET /:id/models`, `POST /:id/validate` all lacked the admin-bypass branch `DELETE /:id` already had. Body: "Not deployed. PR only, per instruction." |
| [#365](https://github.com/jonhill90/hill90-app/pull/365) | Part of #364 (chat-path trace, still open). Agentbox already failed fast locally when `CHAT_CALLBACK_TOKEN` was unset; the API side had no matching check, so it dispatched anyway and the message sat pending until the stale sweeper reported the wrong cause ("Response timed out") 2+ minutes later. Body: "Not deployed — PR only, per repo convention." |
| [#366](https://github.com/jonhill90/hill90-app/pull/366) | Separate real fix, same file as #365, found incidentally while working it. The dispatch-failure path computed the real cause into a local `reason` variable, logged it, then discarded it and persisted the fixed string `'Dispatch failed'` to the DB. A pre-merge comment (02:42:26Z, four minutes before merge) says "Not deployed" — that was true at the time it was written, not a claim about after merge. |
| [#367](https://github.com/jonhill90/hill90-app/pull/367) | Verification only, no production code changed. Answers #364's one flagged open question — whether the SSE `Last-Event-ID` reconnect cursor is correct against a shared-seq, multi-UPDATE-path scenario. Driven through the real route handler with a non-vacuous control, in an isolated worktree. Finding: the mechanism is correct. |
| [#371](https://github.com/jonhill90/hill90-app/pull/371) | Monitoring's Cost card read `usage.total_cost`; the API returns `total_cost_usd`. `undefined ?? 0` rendered $0.0000 unconditionally since the widget was written — invisible only because `model_usage` has zero production rows, so $0 was coincidentally correct. Also fixed adjacent Requests/Tokens cards missing the same `Number()` wrap. Body: "Not deployed, per instruction." |
| [#373](https://github.com/jonhill90/hill90-app/pull/373) | Companion to #371, same parent issue #370. `distinct_models` was never computed by the API at all — not a rename, a genuinely unfinished field, same `?? 0` shape hiding it. Decided to compute it (`COUNT(DISTINCT model_name)`) rather than remove the card. Body: "Not deployed, per instruction." |
| [#372](https://github.com/jonhill90/hill90-app/pull/372) | This session's own work, detailed in full earlier in this conversation: encrypts `mcp_servers.connection_config` (app#369) matching `provider_connections`' existing AES-256-GCM pattern, whole-blob not per-key, plus a second commit closing a UI break the encryption caused (the list read the now-absent field, the edit form would have silently blanked a stored config on save). Jon confirmed directly after merge: deployed, and verified on the host — `mcp_servers` now has `connection_config_encrypted`/`connection_config_nonce`, both `bytea`, plaintext column gone. |
| [Hill90#718](https://github.com/jonhill90/Hill90/pull/718) | Corrected Hill90's own CLAUDE.md: a "zero rows" claim about `agents`/`chat_threads`/`provider_connections` that the same author had personally invalidated hours earlier by seeding QA data. Live counts recorded, `Verified 2026-08-05 01:05 UTC`. |
| [Hill90#719](https://github.com/jonhill90/Hill90/pull/719) | Dropped a never-provisioned `TENANT_BASELINE_TOKEN` checkout-token reference that had made `tenant-baseline-agreement.yml` fail on every run since it was created — see below, this is the one-line fix behind the workflow's first-ever pass. |

### Deploys: what the tracker shows, and what was actually read off the running host

Every one of the 11 hill90-app PRs above has a matching `Manual Deploy App
(Prod)` run — confirmed via `gh run list` — that started within roughly one to
two minutes of that PR's merge time, and every one of those runs completed
`success`. That much is directly verifiable from the tracker: a deploy really
did follow each merge, on the cadence described.

Several of the PR bodies say the opposite of "checked on the host" — #358,
#359, #363, #365, #371, and #373 all contain some form of "Not deployed — PR
only, per instruction" **as of when that text was written**, which was before
the matching deploy run in every case (#366's "Not deployed" comment, for
example, is timestamped 02:42:26Z, four minutes before the PR merged and the
02:46:19Z deploy ran). Those lines describe what the PR's own author did at
authoring time, not what happened afterward — they are not evidence against a
later host check, but they are not evidence for one either. That distinction
— a deploy going green is not the same claim as someone having read the
running artifact — is the reason this subsection exists, and it is the one
piece of this document not to soften.

What follows closes the gap the first draft of this section correctly refused
to paper over: ten host reads, run against the running container after each
deploy completed, not against the repository. Posted to this PR as a comment
and folded in here with citation, since a claim without the command and its
output is exactly what the rest of this document has been holding every other
PR to.

| PR | Host check, after deploy | Result |
|---|---|---|
| #354 | `grep -o "stats\.collections\|graph-counts"` in the shipped UI chunk | `graph-counts` present, `stats.collections` **0 hits** (was 2 before) |
| #357 | `grep -c AI_SERVICE_URL /app/dist/services/model-router-token.js` | 3 |
| #358 | `grep -rlo tasks-truncated-notice /app/.next/static/chunks/*.js` | present |
| #359 | `grep -c "created_by IS NULL" /app/dist/routes/provider-connections.js` | 4 |
| #363 | `grep -c fetchOwnedConnection` + residual-scope check | 4 sites; only 2 `id = $1 AND created_by = $2` left, both the correct non-admin branches (helper line 43, DELETE line 285) |
| #365 | `grep -c callback_not_configured /app/dist/routes/chat.js` | 2 |
| #366 | `grep -c sanitizeDispatchReason` and `safeReason` | present; `safeReason` at all 3 sites, so the log ships redacted too |
| #371 | `total_cost_usd` present, bare `total_cost` | **0 hits** |
| #373 | `grep -c "COUNT(DISTINCT model_name)" /app/dist/routes/usage.js` + ran the aggregate against `model_usage` | 1; query executes, column name real |
| #372 | `information_schema` on `mcp_servers` | `connection_config` **gone**; `connection_config_encrypted` and `connection_config_nonce`, both `bytea` |
| hill90-app#353 (agentbox image, not a code PR) | `com.hill90.revision` label read off the built image, plus a grep for both fix markers inside it | label = `9a2dcf38179840b548791a56391c1a0315b23c73`, matching `origin/main` at build time; `stdout_truncated` ×4 in `policy.py`, `logger.warning` in `ws_terminal.py` — full detail below, not just the build's green tick |

Independently cross-checked the ten **PR** rows above (everything but the
agentbox image row, which is covered on its own terms below) against the
merged diff before folding them in — not by reading the host (no access to it
from here), but by confirming the exact strings, test IDs, and identifiers
cited above appear in what actually merged: `graph-counts`/
`data.stats.collections` in #354, `AI_SERVICE_URL` in #357,
`tasks-truncated-notice` in #358, `created_by IS NULL` in #359,
`fetchOwnedConnection` plus the two residual `id = $1 AND created_by = $2`
sites in #363, `callback_not_configured` in #365,
`sanitizeDispatchReason`/`safeReason` in #366, `total_cost_usd` in #371,
`COUNT(DISTINCT model_name) AS distinct_models` in #373. Every one is
consistent with the source that shipped. **No disagreement found between
these ten PR rows and the tracker.** That is corroboration that the code
these reads are checking is the code that actually merged — not a second,
independent confirmation that the host reads themselves were run as
described, which this document has no way to verify from here. (The agentbox
row's revision hash was separately corroborated against hill90-app#353's own
issue thread in the section below, which recorded the same hash and the same
two fix-marker counts before this comment arrived.)

**#367 needs no host check, and is not part of the "unverifiable" set above.**
It is test-only — no production code changed — so there is nothing in a
running artifact to look at. The table earlier in this section already calls
it verification-only; this section agrees with it rather than leaving it
ambiguous.

**Restated with the new evidence folded in: deploy-confirmed for all 11,
host-artifact-confirmed-with-citation for 10 of the 11 that could have one
(#354, #357, #358, #359, #363, #365, #366, #371, #373, #372), needing none for
the 11th (#367).** That is a materially stronger claim than the first draft of
this section could support, and it is stronger specifically because the
evidence is now cited rather than asserted.

### The workflow that finally passed — and it is a different "first" than the watchdog

Two distinct things happened to two distinct workflows tonight, and the first
draft of this document ran them together. Neither is in this lane's PR list
above, so both are background, not part of the 13 PRs — stated because the
second one is what Jon's original point 4 asked about specifically.

**Hill90's own scheduled-workflow watchdog** (#714–#716, merged 19:14–19:34Z,
outside this lane's window) delivered its first successful alert at
19:35:43Z — a POST to a real, running Alertmanager container reporting that a
*different* workflow, `Tenant Baseline Agreement`, had failed. That is the
watchdog doing its job: noticing a red run and saying so.

**The `Tenant Baseline Agreement` workflow itself** is what #719 (03:01:28Z, in
this lane) fixed — a one-line deletion of a `TENANT_BASELINE_TOKEN` checkout-
token reference that had never been provisioned, which had made every run of
`tenant-baseline-agreement.yml` fail since the workflow was created (confirmed
via `gh run list`: six prior runs, back to 2026-08-03T22:49:45Z, every one
`conclusion: failure`). Run `30971038130`, at **03:01:31Z**, is the first
`conclusion: success` this workflow has ever produced.

**This run is also the concrete case for why a step list is not enough,
demonstrated once rather than asserted repeatedly.** Independently pulled the
step list for this exact run (`gh run view 30971038130 --json jobs`): every
step's conclusion, including `Compare the two lists`, reads `success`. A
workflow whose comparison step silently no-ops — skips the real check and
exits 0 — would produce an identical step list. The only thing that
distinguishes a real pass from a silent no-op is the step's actual stdout, and
reading it directly (`gh run view 30971038130 --log`) shows the comparison
genuinely ran and produced a real result:

```
Tenant baseline agrees: 16 containers, both lists identical.
```

That comparison step had never executed successfully in this workflow's entire
history before this run — not "fixed a workflow" in the sense of restoring
something that used to work, but a check that existed for its whole life
without ever once producing a result, finally producing one. And the step
list alone, without the stdout, would not have told you that.

### The agentbox rebuild — with a headline number corrected in its own thread

hill90-app#353, closed. The drift alarm's own report read "74 commit(s) behind
origin/main," and an earlier draft of this section repeated that number as the
whole picture. **The issue's own later comment corrects it**: 81 total commits
separated the built image from `origin/main`, but only **five** touched
`services/agentbox` — `chat.py` (+77), `policy.py` (+74), `runtime.py` (+19),
`shell.py` (+14), `ws_terminal.py` (+2). "74 commits behind" (now 81) described
the repo; the rebuild this issue argues for is much smaller than that number
suggests. Three of the five changed files carry the same "reports success,
isn't" defect family the rest of tonight's work has been hunting elsewhere.

Rebuilt via `Build Agentbox Images (Prod)` (run `30969169447`). Verified
directly on the host, not from the workflow's tick: the new image's
`com.hill90.revision` label reads `9a2dcf38179840b548791a56391c1a0315b23c73`,
matching `origin/main` HEAD at rebuild time, and a grep **inside the running
image's files** confirmed the actual fix content is present (`stdout_truncated`
in `policy.py`: 4 hits, `logger.warning` in `ws_terminal.py`: 1 hit) — not just
a newer label on old bytes. Safe mid-QA per #228's image-revision stamping (a
running agent keeps its original image); no agent container has ever existed
on this host, so there was no established behavior a rebuild could disturb.

### Negative results — four separate "we looked and it was fine," do not redo these

- **The #354-class field-mismatch sweep — clean.** hill90-app#356 (closed,
  report-only, no PR). Enumerated the response shapes `services/knowledge` and
  `services/ai` actually return against every field the TypeScript consumers
  read, endpoint by endpoint, verified live against the local stack where
  possible. "Confirmed mismatches: none found, beyond the one #354 already
  fixed." (#371/#373 later independently re-checked the same class for
  `usage.*` specifically and also found nothing further — a second, narrower
  confirmation of the same negative.)
- **Fire-and-forget in TypeScript — clean.** hill90-app#362 (closed, report-
  only, no PR). This is the TypeScript-side counterpart to hill90-app#347's
  Python sweep, done live against the current tree: "TypeScript fire-and-forget
  sweep (services/api, services/ui) — #133's class, checked live: clean." Its
  own text is explicit that `services/api` had already been substantially swept
  by #133/#135/#291 and re-verified rather than assumed still-holding. **This
  does not mean the fire-and-forget class was clean everywhere tonight** — #347
  (Python, outside this lane's PR list, but the finding is relevant here) found
  two real defects: one fixed in #348 (agentbox's PTY-reader log call silently
  discarded by the level filter), one still open as a 3-option decision for Jon
  (agentbox's background token-refresh threads dying silently on an uncaught
  exception). Record precisely: TypeScript clean, Python not — don't merge the
  two into one "fire-and-forget: clean" line.
- **The SSE reconnect cursor — verified correct.** hill90-app#367, test-only, no
  production code changed. See the PR table above; don't cite this alongside
  #365/#366 as if all three were the same kind of result — those two are real
  fixes to a different part of the same #364 chat-path trace.
- **Provider-key validation — reachable and persisted.** hill90-app#360
  (closed, report-only, no PR). Traced end to end from two real UI buttons
  (`ConnectionsClient.tsx`'s per-connection "Test" and "Check All") through
  `provider-connections.ts` to `services/ai`'s `POST /internal/validate-
  provider`, confirmed both paths write `is_valid` back. Filed and closed the
  same night specifically to rule out the shape Hill90#712 found elsewhere (a
  probe built and never switched on) — this is not that shape.

### Still open, from this lane specifically

- hill90-app#364 — its one stated open question (does `CHAT_CALLBACK_TOKEN`
  actually hold a non-empty value in `app-api`'s live environment) was never
  answered; #365/#366 fixed the failure mode regardless of the answer.
- hill90-app#347 Defect 2 (agentbox token-refresh threads) — Jon's decision,
  3 options laid out, no recommendation made.
- hill90-app#369 — #372 closes the encryption finding; two other findings in
  the same issue (no write path into `agent_mcp_servers` exists anywhere, and
  `services/mcp` implements nothing) are unaddressed, filed as report-only.

### The recurring lesson, named once

Five separate defects across this lane and the adjacent #354-class sweep were a
consumer reading a field the producer does not send, made invisible by a
defensive default: #354 (`data.stats` vs. the API's `total`), #359
(`WHERE created_by = $1` silently excluding `created_by IS NULL` rows), #371
(`usage.total_cost` vs. `total_cost_usd`), #373 (`distinct_models`, never
computed, not even a rename), and #372's own UI half (the list reading
`connection_config` after the API stopped sending it, guarded into a blank
render instead of an error). Every one shipped invisibly, some for months,
because `?? 0`, `|| {}`, or `|| ''` turned a missing field into a plausible-
looking zero or blank instead of a thrown error. **This is now the most
productive shape to grep for in this codebase**: any place a frontend or a
downstream route reads a field with a fallback and check whether the value it
falls back *from* still exists on the producing side. A fallback is not a bug
by itself — #358's `totalFrom()` uses one correctly — the bug is a fallback
masking a field that used to exist and no longer does, or was promised and
never shipped.
